<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Feats" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <!-- Main Navigation -->
    <%= render 'layouts/navigation' %>
    
    <div class="admin-area">
      <div class="row">
        <h1 class="admin-title">Admin Area</h1>
        <!-- Side Bar -->
        <div class="columns large-4 admin-side">
          <ul class="side-nav">
            <li><%= link_to 'List Badges', badges_path %></li>
            <li><%= link_to 'Add Badge', new_badge_path %></li>
          </ul>
        </div>
        <!-- Content -->
        <div class="columns large-8">
          <%= yield %>
        </div>
      </div>
     </div>    
    </div>

     

    <%= javascript_include_tag "application" %>
  </body>
</html>